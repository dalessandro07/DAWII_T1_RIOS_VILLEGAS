server:
    port: 8081

spring:
    application:
        name: dawii-t1-consumer
    
    datasource:
        url: jdbc:mysql://localhost:3306/dawii_t1
        username: root
        password: ${DB_PASSWORD:root}
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
